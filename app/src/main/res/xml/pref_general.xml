<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">



    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="@string/pref_sortingorder_default"
        android:entries="@array/pref_sortingorder_titles"
        android:entryValues="@array/pref_sortingorder_values"
        android:content="@string/pref_key_sortingorder"
        android:title="@string/pref_title_sortingorder" />

</PreferenceScreen>
